/// [Enum] used to loop audio player according to user preference
enum RepeatPlaylist {

  /// This will set audio player to [not] repeat any audio
  none,

  /// This will set audio player to repeat current audio [once]
  one,
  
  /// This will set audio player to repeat [all] available audio file once
  all,
}